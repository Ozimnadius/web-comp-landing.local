# Ассеты

Выгружено из Figma [Web Comp — Landing](https://www.figma.com/design/1lQxYxZfvOiCg1IrwC4f9X/Web-Comp---Landing--01.09.2026-) через MCP.

Раскладка: `svg` → `icons/`, растр → папка своей секции (`hero/`, `portfolio/`, `about/`, `brief/`). В репозитории только то, что уходит в браузер: `webp` и `svg`. Исходные `jpg`/`png` не хранятся — выгружаются из Figma заново по таблице ниже.

## Что откуда

| Файл | Node | Где |
|---|---|---|
| `icons/logo.svg` | `3791:699` | логотип шапки сайта, «web» белым |
| `icons/logo-dark.svg` | `5849:8631` | логотип шапки брифа и шапки мобильного меню, «web» тёмным, 112×20 |
| `icons/menu.svg` | `5709:1528` | бургер, с 768 |
| `icons/phone.svg` | `5715:2361` | иконка телефона: в шапке сайта только на 375, в шапке мобильного меню всегда |
| `icons/close.svg` | `7765:2399` | крестик мобильного меню (24) и лайтбокса кейса (14 в белом круге) |
| `icons/bitrix-partner.svg` | `3791:745` | полоса метрик |
| `icons/check.svg` | `3791:801` | галочка в списке тарифа |
| `icons/price-card-ellipse-1…2.svg` | `3791:822`, `3791:823` | эллипсы тёмной карточки тарифа |
| `icons/social-telegram.svg` | `3791:729` | hero, мобильное меню |
| `icons/social-max.svg` | `3791:733` | hero, мобильное меню |
| `icons/arrow-up.svg` | `5715:2303` | «Наверх» в подвале, бокс 80×44,59 вместе с полями |
| `icons/footer-logo-watermark.svg` | `5715:2333` | вотермарк подвала и мобильного меню, вся надпись 1770×334,675 |
| `icons/close-circle.svg` | `5846:5270` | «Закрыть» в шапке брифа |
| `icons/chevron-left.svg` | `5843:1938` | «Назад» в подвале брифа; «Вперед» — тот же файл, зеркалится трансформом |
| `icons/check-bold.svg` | `I5848:5933;5843:1967` | заголовок «Нравятся» на шаге 3 и пройденные шаги степпера |
| `icons/minus.svg` | `5848:6006` | заголовок «Не нравятся» на шаге 3 |
| `icons/radio-blank.svg` | `5849:6277` | пустой radio на шаге 4 |
| `icons/checkbox-marked.svg` | `5849:8765` | отмеченный чекбокс согласия на шаге 5 |
| `brief/type-business-card.webp` | `5846:3335` | карточка «Сайт визитка», шаг 1 |
| `brief/type-corporate.webp` | `5846:4444` | карточка «Корпоративный сайт» |
| `brief/type-shop.webp` | `5846:4559` | карточка «Интернет магазин» |
| `brief/type-landing.webp` | `5846:4831` | карточка «Лендинг» |
| `brief/promotion.webp` | `5849:7258` | иллюстрация шага 4 «Продвижение и реклама» |
| `brief/done.webp` | `5849:11164` | иллюстрация экрана «Спасибо» |
| `hero/bg-*.webp` | выгружено дизайнером | фон первого экрана, см. ниже |
| `hero/hero-img.webp` | `5722:2844` | ноутбук, только 375 |
| `portfolio/bodo-*` | `3791:855` | кейс BODO |
| `portfolio/lingua-large` `-small` | `3791:876` | превью кейса Lingua Planet в карточке |
| `portfolio/lingua-full.webp` | `7765:2139` | полный скриншот сайта в лайтбоксе карточки 02, 1192×4096 |
| `portfolio/maxirolls-*` | `3791:877`, `7765:2140` | кейс Maxi Rolls; оба узла отдают один и тот же файл, см. ниже |
| `portfolio/olato-large` `-small` | `3791:878` | превью кейса Olato в карточке |
| `portfolio/olato-full.webp` | `7765:2142` | полный скриншот сайта в лайтбоксе карточки 04, 1484×4096 |
| `about/map-large.webp` `map-small.webp` | `3791:897` | карта |

Подключаются CSS-маской: `menu.svg`, `phone.svg`, `check.svg`, `close.svg`, `social-telegram.svg`, `social-max.svg` и все шесть иконок брифа — `close-circle.svg`, `chevron-left.svg`, `check-bold.svg`, `minus.svg`, `radio-blank.svg`, `checkbox-marked.svg`. Цвет задаёт `background-color`, собственная заливка файла не читается — в иконках брифа и в `close.svg` она так и осталась той, какой её отдала Figma (`black`, `#C5C5C5`).

## Фон hero

Шесть файлов под шесть полос, все в 1.5×. **На 768 и выше ноутбук и эллипсы впечатаны в фон; на 375 — только фон**, ноутбук вынесен в `hero-img.webp`.

| Файл | Размер | Полоса |
|---|---|---|
| `bg-375.webp` | 556×917 | `max-width: 767.98px` |
| `bg-768.webp` | 1152×775 | `max-width: 1199.98px` |
| `bg-1200.webp` | 1800×954 | `max-width: 1439.98px` |
| `bg-1440.webp` | 2160×1079 | `max-width: 1599.98px` |
| `bg-1600.webp` | 2400×1251 | `max-width: 1919.98px` |
| `bg-1920.webp` | 2880×1392 | выше 1919.98 |

Подключены через `<picture>` с теми же брейкпоинтами. У `<img>` стоит `fetchpriority="high"` и нет `loading="lazy"`: первый экран, кандидат в LCP.

## Картинки брифа

Шесть файлов в `brief/`: четыре карточки типа сайта по 387×365, `promotion.webp` 904×836 и `done.webp` 713×582. Все в 2× от макетного размера.

Выгружались через `download_assets` MCP Figma: обычный `get_screenshot` отдаёт только 1×, для ретины его не хватает. Оттуда приходит PNG, в `webp` переведён PIL с качеством 85.

**Альфа-канала в файлах нет, фон запечён в картинку.** У карточек типа запечено и скругление: углы белые, а не прозрачные, — повторять радиус в CSS не нужно, но и подложку под карточкой перекрасить нельзя, картинка ляжет верно только на белом. У двух иллюстраций так же зашит светло-серый фон (`#f1f3f4` у `promotion`, `#f2f4f5` у `done`).

Логотип в шапке брифа — свой файл `icons/logo-dark.svg`, вариант «Frame 227» 112×20 (`5849:8631`). Сначала переиспользовали `icons/logo.svg`, и в браузере оказалось, что слово «web» в нём залито белым: на белой шапке брифа от знака остаётся половина. Маской знак не перекрасить — он двухцветный и идёт `<img>`, как логотип шапки сайта.

Цвет в тёмном варианте зашит в файл — `#14171C`, те же грабли, что с вотермарком подвала: смена `--Black` его не тронет.

## Особые случаи

**`portfolio/maxirolls-long.webp`** — `1921×3996`. Не ошибка экспорта: в макете картинка обрезана по верху и показана в боксе `863/577`. Прежняя запись связывала эту длину с прокруткой из видео-референса — связь ошибочная: в видео прокручивается кейс BODO, а не Maxi Rolls, и делает это теперь `video`, а не длинная картинка.

Этот же файл служит и лайтбоксу карточки 03: узел `7765:2140` из правок 03.09.2026 содержит ровно тот jpeg, что уже лежал здесь, — своего `maxirolls-full.webp` не заводили. При повторной выгрузке сверять не по имени узла, а по размеру.

**`portfolio/lingua-full.webp` и `portfolio/olato-full.webp`** — полные скриншоты сайтов под лайтбокс карточек 02 и 04, узлы `7765:2139` («Главная 2») и `7765:2142` («SamcoNew 1»). Имя второго узла — от чужого проекта: внутри Olato, сверено с превью `olato-large.webp`. **По именам этих узлов при повторной выгрузке не ориентироваться.**

Оба выгружены через `download_assets` MCP Figma в режиме `rawImages` — это исходные jpeg заливок, а `get_screenshot` отдал бы перерисовку узла. В `webp` переведены PIL с качеством 80.

Высота у обоих ровно `4096` при разной ширине — похоже на потолок, а не на совпадение, так что более длинного варианта из Figma ждать не стоит. На результат это не влияет: в лайтбоксе картинка показывается шириной 1024.

## Видео

Лежит вне `images/`, в `upload/video/`.

| Файл | Что | Размер |
|---|---|---|
| `video/bodo.mp4` | превью главного кейса портфолио | 1440×810, 25 с, 1,59 МБ |
| `images/portfolio/bodo-poster.webp` | постер к нему, первый кадр ролика | 1440×810 |

Исходник дизайнера — 1920×1080, 25 с, 24,3 МБ со звуковой дорожкой. Приведён к весу лендинга: снят звук, масштаб до 1440, `libx264 -crf 30`, `+faststart`. Обрезки по времени нет — заставка с логотипом в начале задумана, а не артефакт загрузки.

**Ролик не зациклен:** начинается верхом сайта, кончается подвалом. Поэтому `loop` не ставится, воспроизведение одно и замирает на последнем кадре.

Соотношение ролика 16:9. Бокс превью в вёрстке подогнан под него, а не под макетные 2,015:1, поэтому ролик не кадрируется вовсе. Постер намеренно взят первым кадром, а он почти белый: так при запуске нет скачка, но до автозапуска на месте превью белый прямоугольник.

**`icons/check.svg`** — обводка переведена с `#FFC600` на `currentColor`. На результат это не влияет: галочка рендерится маской, и заливка из файла не читается вовсе. Цвет приходит из `background-color: var(--Yellow)`.

**`icons/check-bold.svg`** — не замена `check.svg`, а второй файл. У брифа галочка залитая и жирная (13,85×13,85), у списка тарифа — тонкая обводка 16×16. Фигуры разные, одна другой не масштабируется.

**`icons/price-card-ellipse-1…2.svg`** — подключены фоном в `::before` и `::after` тёмной карточки, а не `<img>`: это декорация, которой нечего делать в разметке.

**Линии-разделители не выгружались** — Figma отдаёт их как SVG, но это границы, делаются `border`.

## Чего нет

- Иконки «плюс/минус» для FAQ (`3791:887`) — Figma не отдаёт как SVG, собираются в CSS.
- Выбранный radio и пустой чекбокс брифа — таких состояний в макете нет вовсе. Рисуются в CSS по образцу выгруженных `radio-blank.svg` и `checkbox-marked.svg`, чтобы обводка и размер совпали с парой.
- Линии между кружками степпера и точка-разделитель в подвале брифа — CSS, из Figma не выгружались.
